name: Terraform Apply

on:
  push:
    paths:
      - 'terraform.tfvars'

jobs:
  terraform:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        environment: [dev, prod, perf]

    env:
      ENVIRONMENT: ${{ matrix.environment }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    
    - name: Debug
      run: |
        echo "Selected Environment: $ENVIRONMENT"
        az account show
        az resource show --ids /subscriptions/$ARM_SUBSCRIPTION_ID/resourceGroups


    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v1

    - name: Set up Azure CLI - Dev
      if: env.ENVIRONMENT == 'dev'
      run: az login --service-principal -u ${{ secrets.AZURE_CLIENT_ID }} -p ${{ secrets.AZURE_CLIENT_SECRET }} --tenant ${{ secrets.AZURE_TENANT_ID }}

    - name: Set up Azure CLI - Prod
      if: env.ENVIRONMENT == 'prod'
      run: az login --service-principal -u ${{ secrets.AZURE_CLIENT_ID }} -p ${{ secrets.AZURE_CLIENT_SECRET }} --tenant ${{ secrets.AZURE_TENANT_ID }}

    - name: Set up Azure CLI - Perf
      if: env.ENVIRONMENT == 'perf'
      run: az login --service-principal -u ${{ secrets.AZURE_CLIENT_ID }} -p ${{ secrets.AZURE_CLIENT_SECRET }} --tenant ${{ secrets.AZURE_TENANT_ID }}

    - name: Terraform Init
      run: terraform init

    - name: Terraform Apply
      run: terraform apply -var-file=terraform.tfvars -auto-approve

      env:
        ARM_CLIENT_ID: ${{ secrets[format('ARM_CLIENT_ID_{0}', env.ENVIRONMENT)] }}
        ARM_CLIENT_SECRET: ${{ secrets[format('ARM_CLIENT_SECRET_{0}', env.ENVIRONMENT)] }}
        ARM_SUBSCRIPTION_ID: ${{ secrets[format('ARM_SUBSCRIPTION_ID_{0}', env.ENVIRONMENT)] }}
        ARM_TENANT_ID: ${{ secrets[format('ARM_TENANT_ID_{0}', env.ENVIRONMENT)] }}

